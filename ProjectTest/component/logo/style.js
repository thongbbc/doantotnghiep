import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  logo: {
    justifyContent: 'center',    alignItems: 'center',
    height:100,width:100,backgroundColor:"rgba(255,164,164,1)",borderRadius:100
  }
});

export default styles;